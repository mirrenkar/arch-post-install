# Arch Linux Post-Install Script (Perfect Edition)

Скрипт для автоматизации установки пакетов и глубокого анализа необходимых системных сервисов. Создан для тех, кто ценит чистоту логов и точность автоматизации.

## ✨ Особенности

* **Двухуровневая установка**: Поддержка официальных репозиториев (`pacman`) и AUR (`paru`).
* [cite_start]**Идеальные логи**: Извлечение чистых версий пакетов без системного «шума» и лишних символов[cite: 1, 3].
* [cite_start]**Динамический поиск сервисов**: Скрипт анализирует содержимое каждого пакета из списка и находит юниты `systemd` (.service, .timer, .path), предлагая их включить[cite: 1].
* **Режим имитации (--dry-run)**: Проверка всех действий без внесения изменений в систему.
* **Интеллектуальный пропуск**: Если пакет или сервис уже настроены, скрипт не будет тратить на них время.

## 📂 Структура файлов

* [cite_start]`arch_post_install.sh` — основной скрипт[cite: 1].
* [cite_start]`needed_app` — ваш список пакетов (формат: одно название на строку)[cite: 1].
* [cite_start]`installed_app.log` — детализированный отчет об установке с версиями[cite: 1, 2].
* [cite_start]`service_needed.log` — рекомендации по активации найденных служб[cite: 1].
* `error_install.log` — журнал ошибок установки.

## 🚀 Использование

### 1. Подготовка списка
Отредактируйте файл `needed_app`, добавив туда нужные пакеты. Комментарии через `#` игнорируются.

### 2. Тестовый запуск
```bash
./arch_post_install.sh --dry-run
